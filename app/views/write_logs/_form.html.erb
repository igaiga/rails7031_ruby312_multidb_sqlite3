<%= form_with(model: write_log) do |form| %>
  <% if write_log.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(write_log.errors.count, "error") %> prohibited this write_log from being saved:</h2>

      <ul>
        <% write_log.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :memo, style: "display: block" %>
    <%= form.text_field :memo %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
